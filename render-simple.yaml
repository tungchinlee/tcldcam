services:
  - type: web
    name: tcldcam
    env: node
    buildCommand: npm ci && npx expo export -p web
    startCommand: node server.js
    region: oregon
    plan: free
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NODE_ENV
        value: production