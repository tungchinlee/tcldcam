services:
  - type: web
    name: tcldcam
    env: node
    buildCommand: npm ci && npx expo export -p web
    startCommand: npx serve dist -l $PORT -s --cors
    region: oregon
    plan: free
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NPM_CONFIG_PRODUCTION
        value: false